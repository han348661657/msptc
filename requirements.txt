# msptc runtime + test dependencies.
# Tested with CPython 3.13.9; Python >= 3.10 recommended.
# Pins are the verified-working lower bounds, not hard upper bounds.

numpy>=2.3
scipy>=1.16
pandas>=2.3
matplotlib>=3.10

# Testing only
pytest>=8.4
